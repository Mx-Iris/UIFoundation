# 术语表

本项目专有名词与约定用法 —— 只收 UIFoundation 特有的：自造词与内部代号、本库特有的缩写、
通用术语在本库里的特定含义、以及容易混淆的近义词对。

跨项目通用的术语（Mach-O、slice、ABI 与源码兼容性之别、entitlement 等）收录在全局术语表中，
本表不重复登记。全局表不在本仓库内，位于 iCloud Global 镜像的 `Documentations/Glossary.md`。

**不收**语言与框架的通用术语（Swift 的 `optional`、AppKit 的 `NSView`），收进来只会稀释。

## 维护约定

提案或专题文章引入新术语时，**同批次**登记进本表。文档里首次出现该术语时展开一次并链到这里，
之后不必每篇重复解释。

## 术语

**暂无词条。**

本表原有的五条（`ExclaveCore / ExclaveKit`、`field 与 column`、`guest 进程`、`style（呈现样式）`、
`platform（本项目含义）`）全部随 RunningApplicationKit 并入时登记，已在 2026-09-05
随 [0014](Evolutions/0014-running-application-merge.md) 的撤销一并移除 —— 它们描述的代码
不在本库中了。需要查阅的话，它们仍在原仓库
[`Mx-Iris/RunningApplicationKit`](https://github.com/Mx-Iris/RunningApplicationKit) 的术语表里。
