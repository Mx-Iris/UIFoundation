# Evolution 提案索引

- **项目类型**: 库（源码分发）

SPM library product，使用方以源码依赖并重新编译，未开启 library evolution，
也不以 `binaryTarget` 分发。

**「ABI 兼容性」一节填「不适用 —— 本库以 SPM 源码分发，使用方每次重新编译」即可；
「源码兼容性」一节必填。**

本库有一条特有的注意事项：**它是多个项目的公共底座，「下游影响」一节必须逐个点名**。
已知下游包括 RuntimeViewer（`TabsControl`）、MachOKitUI（`TextFinder`）、
PrivateSymbols（全面改用本库基类）。改动一个组件的行为，等于同时改动这些项目的行为。

另外，本库既有组件的对外契约写在使用指南里（见[上级索引](../README.md)）。
提案若改动了这些契约，**必须在同一批次更新对应指南** —— 契约变了而指南没变，
比没有指南更糟。

提案格式与流程见全局 `CLAUDE.md` 的「Evolution 提案制」一节，用 `/evolution <描述>` 创建。

## 提案

| # | 标题 | 状态 | 摘要 |
|---|------|------|------|
| [0001](0001-appstore-style-navigation-controller.md) | NavigationController：移植 macOS App Store 的导航容器与推入/弹出转场 | Implemented | AppKit 没有导航容器，本提案把 macOS App Store 自己那套搬进来：视图控制器栈、推入/弹出转场、双指右滑返回。落在新 trait `Navigation`（默认关闭）下，对现有使用方零影响。配套使用指南见 [`Navigation.md`](../Navigation.md)，逆向依据见 [`Researchs/AppStore-Custom-Navigation-Internals.md`](../../Researchs/AppStore-Custom-Navigation-Internals.md)。 |
| [0002](0002-reusable-settings-window.md) | SettingsWindow：把 RuntimeViewer 的设置窗口抽成可复用框架 | Implemented | 每个 macOS App 都要重写一遍的设置窗口壳：分页导航、`Form`、Application Support 下的 JSON、改了就自动存的防抖。从 RuntimeViewer 抽出与业务无关的那 250 行，做成泛型 `SettingsStore`、泛型化的 `AppSettings` 属性包装器（store 走类型级静态入口，不走 Environment）、result builder 注册页面。分模型层 `UIFoundationSettings` 与 UI 层 `UIFoundationSettingsUI` 两个 target，均仅 macOS，落在新 trait `Settings`（默认关闭）下，**不引入任何新外部依赖**。配套使用指南见 [`SettingsWindow.md`](../SettingsWindow.md)。RuntimeViewer 回接尚未进行，先决条件见提案「下游影响 ⓪」。 |
| [0003](0003-settings-navigation-history.md) | SettingsNavigator：设置窗口的页面历史导航 | Implemented | 设置窗口加一对 Xcode 式的前进 / 后退 chevron，并把「当前在哪一页」从 `SettingsRootView` 的私有 `@State` 里取出来，做成宿主可以在代码里直接读写的 `SettingsNavigator`。历史是唯一事实来源、侧栏选中项由它派生，避开了「选中项 + 标志位」那类时序 bug。子页面下钻不做 —— 实测页内直接写 `NavigationStack` 已自带返回按钮。纯新增，仅动 `UIFoundationSettingsUI` 一个 target。 |
| [0004](0004-appkit-navigation-toolbar-item.md) | NSToolbar.Navigation：AppKit 的后退 / 前进工具栏项 | Implemented | AppKit 没有任何内建的导航工具栏项（SwiftUI 侧有，见 0003），RuntimeViewer 已手搓了一个。把它抽进本库既有的 toolbar DSL，重点不是搬那三十行代码，而是把它踩出来的两条 AppKit 行为契约固化进 API 形状：分段菜单只有在控件 `action` 非 nil 时才走长按弹出，以及空 `NSMenu` 仍会弹出空盒子所以必须整个摘掉。数据走 data source 拉取（借 `NSToolbarItem.validate()` 的验证周期），事件走 delegate 通知，条数与行内容分两级以避免逐行图标解析跑在热路径上。纯新增，仅动 `UIFoundationAppKit`，不加 trait。落地时实测**证否了第三条「踩坑」**（挂菜单不会带出下拉箭头）；`NSSegmentedControl` 最终**完全不暴露**，也不提供任何样式转发 —— 观感由库定死，宿主只接数据与回调，这样契约一才真的没有入口可破。配套使用指南见 [`ToolbarNavigation.md`](../ToolbarNavigation.md)。 |
| [0005](0005-observable-settings-model.md) | SettingsStore：改用可观察引用模型并保留属性级失效 | Implemented | 把 0002 的值类型根 Store 改成 `@Observable` class 模型：业务监听按顶层属性失效，Store 则通过模型明确列出的 `accessPersistedValues()` 独立监听所有编码字段并防抖保存。用 generation 处理 Observation 的 willSet 回调、加载换对象与显式保存竞态；RuntimeViewer 同批接回，`transformer` 不再被 `theme` 修改唤醒。 |
| [0006](0006-settings-window-configuration.md) | SettingsWindowController.Configuration：集中管理设置窗口自定义项 | Implemented | 用单一 `Configuration` 收拢窗口标题、窗口尺寸、侧栏宽度、侧栏图标尺寸与导航按钮开关；RuntimeViewer 可显式恢复迁移前的 15 pt glyph 尺寸。 |
| [0007](0007-settings-scene.md) | SettingsScene：让 SwiftUI 与 AppKit 共用原生设置 Scene | Implemented | 在 window controller 与可嵌入 root view 之外增加真正的 SwiftUI `Settings` Scene；SwiftUI App 可直接声明，macOS 26+ 的 AppKit App 可通过 `NSHostingSceneRepresentation` 注册和打开。 |
| [0008](0008-top-level-settings-configuration.md) | SettingsConfiguration：把共享配置移出 Window Controller | Implemented | 配置同时服务 controller、scene 与 root view，不再嵌套于任一呈现入口；删除尚未发布的旧嵌套名称，不保留 alias。 |
| [0009](0009-standard-main-menu.md) | MainMenu：纯代码 macOS App 的标准主菜单 | Implemented | AppKit 没有 API 能生成 MainMenu.xib 那份标准主菜单，纯代码工程只能手抄或留一个只装菜单的 xib。在既有 `@MenuBuilder` DSL 之上新增 `MainMenu` 命名空间：`MainMenu.standard()` 一行拿到模板等价主菜单，`MainMenu.menu { … }` 按顶层菜单 / 单个标准项两级粒度自定义；`windowsMenu` / `helpMenu` / `servicesMenu` / font menu 在装配时按 identifier 自动接线，工厂零副作用。纯新增、无 trait；示例 App 已换用并删除了仅为主菜单保留的 storyboard。配套使用指南见 [`MainMenu.md`](../MainMenu.md)。 |
| [0010](0010-main-menu-builder.md) | MainMenu.Builder：按 identifier 增删改标准主菜单 | Implemented | 0009 里改 `standard()` 的单个项要把整个菜单重列一遍。仿 UIKit `UIMenuBuilder` 的 Swift 接口（`NS_SWIFT_NAME` 形态）加一个变换层：`ItemIdentifier` 改为 struct 并给全部约 90 个标准项打 id，`MainMenu.standard(customizing:)` 把建好的树交给 `MainMenu.Builder` 做查询 / 四向插入 / 替换 / 删除，变换先于接线执行，孤儿分隔符收尾规整。UIKit 三类元素（组 / action / command）在 NSMenu 里坍缩为 item 一类，故三套寻址合并为一套、不做 selector 通道。小范围源码破坏：`ItemIdentifier` 常量类型变化、`windows` 更名 `window`（旧名弃用保留）。 |
| [0011](0011-welcome-panel.md) | WelcomePanel：把 WelcomeKit 的 Xcode 式欢迎窗口移植进本库 | Implemented | 把作者自己的独立仓库 `Mx-Iris/WelcomeKit`（Xcode 式欢迎窗口：左侧图标 / 版本 / 三个操作，右侧最近项目列表，三种样式）整体搬进本库，落在新 trait `WelcomePanel`（默认关闭）下，仅 macOS。公开 API 收进 `WelcomePanelController` 一个顶层符号，其余全部嵌套；内部重复件（图层背衬视图基类、`Then`、`ConstraintMaker`、`ArrayBuilder` 等约 500 行）换成本库已有的基类与 `.box` 扩展 —— 其中四个类名与本库顶层符号直接冲突，去重是搬迁的前提而非可选项。**运行时行为一律保持原样**，包括 `.xcode26` 样式已知的两处漏判（关闭按钮无图标、操作项点按无高亮），只记录不修。 |
| [0012](0012-xcode26-faithful-style.md) | WelcomePanel `.xcode26`：按实测重做成真正的 Xcode 26 复刻 | Implemented | 0011 搬进来的 `.xcode26` 只是 `.xcode15` 的几何去掉毛玻璃，不对应任何真实 Xcode 版本。依据 Xcode 26 欢迎窗口的 view hierarchy 抓包逐项实测重做：圆角 8→20、两侧改用 `NSVisualEffectView(.fullScreenUI)`（**实测其滤镜链与抓包逐位一致，故不需要私有 API**）、标题 30→36 bold、操作项圆角 8→18 胶囊、行内间距与首行位置对齐、图标蓝色辉光变默认；并顺带修掉 0011 故意保留的两处漏判（关闭按钮无图标、点按无高亮）。`.xcode14` / `.xcode15` 不动，公开 API 形状不变，但**观感是破坏性变更**。逆向依据见 [`Researchs/Xcode26-WelcomeWindow-Internals.md`](../../Researchs/Xcode26-WelcomeWindow-Internals.md)。深浅两份抓包均已实测：**几何在两种外观下逐帧一致，只有配色变，且图标辉光仅深色存在**。 |
| [0013](0013-main-menu-factory-customizing.md) | MainMenu：每个多项子菜单的工厂都提供 customizing 参数 | Implemented | 0010 只给 `standard()` 装了 `MainMenu.Builder`，于是「按 identifier 改单个项」这项能力只存在于「整根菜单栏」这一个粒度上：用 `menu {}` 自选顶层菜单的宿主拿不到 builder，`Edit.find()` / `Format.font()` 这类分组子菜单更是毫无入口，想改一项就得整份重列（连带复制那些抄错也不报错的 tag 与 selector）。本提案把同一个 `customizing:` 参数补齐到全部 15 个**产出多项子菜单**的工厂上，并把 Builder 的寻址根从 `NSMenu` 扩展到「菜单项自身 + 其 submenu 子树」，使无 `title` 参数的分组标题也改得到。判据是「submenu 有多个 item 才给」，故排除约 70 个单项工厂、空 submenu 的 `Application.services()` 与只有一项的 `File.openRecent()`。纯新增，不动任何既有签名；三重载下四种闭包形态的消歧已实测。 |
| [0014](0014-running-application-merge.md) | RunningApplication：把 RunningApplicationKit 整体并入本库 | Withdrawn | **已于 2026-09-05 撤销** —— RunningApplication 整体移出本库（target、trait、product、测试、示例与配套文档全部删除），撤销范围记在提案的「撤销」一节；需要这套能力的项目回到原仓库 `Mx-Iris/RunningApplicationKit`。以下为撤销前的原摘要：把作者自己的独立仓库 `Mx-Iris/RunningApplicationKit`（运行中的应用与 BSD 进程的值类型模型、两个观察者、带搜索与排序的选择器）整体搬进本库，落在新 trait `RunningApplication`（默认关闭）下的**独立 target** `UIFoundationRunningApplication`，仅 macOS 11+ —— 不进伞包，因为它的平台下限高于伞包的 10.15。分两阶段落地：先原样搬运（与原库逐行可比，0 处预期外差异），再接入本库基座重写（`.box.makeView`、`TableCellView`、`makeConstraints`、`HStackView`、`XiblessViewController`）。**重写前先把布局测试扩到覆盖每个 cell 的关键几何并确认其在旧实现上全绿** —— 原库有四个布局 bug 编译干净、测试全过却只在截图里现形。顺带删除原库标注「下个 minor 移除」的弃用别名。**本次只做本库这一侧**：原仓库原地保留，四个下游（RuntimeViewer / HopperMCP / MachInjector / LookInside）的迁移与原仓库归档另开一轮。配套使用指南见 `RunningApplication.md`（已随撤销删除）。 |
| [0015](0015-simulator-platform-detection.md) | 进程平台识别与模拟器标记 | Withdrawn | **已随 0014 于 2026-09-05 撤销。** **随 0014 从 RunningApplicationKit 并入，原编号 0001，正文保持原貌。** 读进程可执行文件的 Mach-O `LC_BUILD_VERSION` 判定它被编译成给哪个平台跑，据此在进程列表里标出模拟器 guest 进程 —— 架构做不到这件事，因为 Apple Silicon 上模拟器里的进程跑的是原生 arm64，与宿主进程完全一致。配套实现说明见 `Internal/PlatformDetection.md`（已随撤销删除）。 |
| [0016](0016-picker-presentation-styles.md) | 选择器呈现样式（表格与列表） | Withdrawn | **已随 0014 于 2026-09-05 撤销。** **随 0014 从 RunningApplicationKit 并入，原编号 0002，正文保持原貌。** 给两个标签页各加一个 `style`，在表格之外新增列表样式（图标 + 标题 + 行内徽章 + 副标题）；两种样式对「不值一提的值」处理相反 —— 列表省略、表格用暗色打印，因为一列空白读起来像坏了。`allowsColumns` 改名 `allowsFields`（该弃用别名已由 0014 删除）。配套实现说明见 `Internal/PresentationStyles.md`（已随撤销删除）。 |
| [0017](0017-appkitplus-layer-backed-view.md) | AppKitPlus：把 LayerBackedView 的基类换成 NSLayerBackedView | Implemented | 引入 [AppKitPlus](https://github.com/AppKitSupportProgram/AppKitPlus-Release)（同作者的 UIKit-to-AppKit 移植框架，二进制分发）作为新 trait `AppKitPlus`（默认关闭）下的可选依赖，trait 开启时把 `LayerBackedView` 的基类从 `NSView` 换成 `NSLayerBackedView`（UXKit `UXView` 的移植），两边重叠的 layer-backing 默认值合一，并让本库全部派生视图拿到 `userInteractionEnabled` 与转场用的 safe-area 冻结。**唯一破坏性代价是包级 macOS 下限 10.15 → 12** —— `binaryTarget` 的平台检查在包依赖图层面，`@available` 与 `#if` 都绕不过（已实测报错原文）；三个已知下游下限都已是 15，实际影响为零。版本约束 `from: "0.2.1"`：0.1.6 的 `NSView (Appearance)` category 声明了 `backgroundColor`，会**跨模块静默盖掉** `LayerBackgroundProviding` 的同名协议属性（连不继承新基类的 `LayerBackedTableCellView` 也中招），且把压缩阻力降到 500；0.2.0 两者皆无。**落地时才发现真正的冲突面另有其类**：AppKitPlus 在 `NSCollectionViewItem` / `NSTableCellView` / `NSControl` / `NSWindow` 等十余个宿主类上也加了成员，而子类声明同名属性会被 Swift 判为**非法 override**（不是遮蔽，`@nonobjc` 也救不了）并**随模块传播到下游**，因此拆 target 隔离无效。踩到三处：`contentView` 推动上游改名（0.2.1 起改叫 `configurationContentView`，本库版本下限随之提高，`XiblessCollectionViewItem` 零改动），`QuickActionBar` 的 `parent` 与示例 app 的 `navigationController` 就地改名。命名约束与升级复查清单写在 `CLAUDE.md`「AppKitPlus」一节。 |
| [0018](0018-layer-backed-view-controller.md) | LayerBackedViewController：LayerBackedView 的控制器对位 | Implemented | 0017 把视图那一半换到了 AppKitPlus 的 `NSLayerBackedView` 上，控制器那一半还留在 `XiblessViewController` / `NSViewController`，于是 trait 开启时页面视图拿到了 layer-backing 默认值与 safe-area 冻结，控制器却拿不到 AppKitPlus 为它准备的六个钩子与 `setView:` 那条接线。新增 `LayerBackedViewController<View: LayerBackedView>` 补上：形状照抄 `XiblessViewController`，基类按 trait 二选一。实测确认接线挂在 `setView:` 而非 `loadView` 上，所以覆写 `loadView()` 装 `contentView` 不丢钩子（测试留了金丝雀）；trait 关闭时只补两个 first-appear 钩子（控制器自己就能驱动），另四个由视图驱动的不补，子类覆写它们必须自己写 `#if`。纯新增，不加 trait，`XiblessViewController` 一行不改。 |
| [0019](0019-toolbar-item-action-and-bordered.md) | ToolbarItem：target/action 下沉到中间基类，isBordered 与 isNavigational 上提 | Implemented | Toolbar DSL 里两样本该属于所有 item 的东西只长在 `NSToolbar.Item` 一个类上：`isBordered` / `isNavigational` 这两个 `NSToolbarItem` 自带的属性，以及 target/action 的链式 setter —— 后者的缺口已经在下游留下疤痕，XCOrganizer 为了给一个 `NSToolbar.Button` 挂 `#selector`，把 item 从 builder 里拉成了局部变量并留注释「`action` has no chainable form」。两个属性上提到 `ToolbarItem`；target/action 连同七份逐字相同的 45 行 trampoline 样板下沉到**新中间基类** `ActionableToolbarItem`，`actionHost` 决定 target/action 落在 item 还是托管控件上。选中间基类而非直接放 `ToolbarItem`，唯一理由是实测「`NSToolbarItem` 的 target/action 会 forward 到 custom view」—— 放基类等于给 `Navigation` 的内部 `segmentedControl` 开后门，与 0004 定死的封闭性冲突，故 `Navigation` / `Search` / `TrackingSeparator` 留在中间基类之外。两个旧 `bordered(_:)` 标弃用。**落地时牵出并修掉一个既有 bug**：属性观察器不为 initializer 内的赋值触发，于是 `Item` 与 `Button` 的 `init(…, action:)` 一直只存闭包、从不接线（`SegmentedControl` 因 `commonInit()` 里补了第二次安装而侥幸正确）；改成计算属性后自动修复，回归测试已验证其在旧形状下变红。同类模式全库排查 8 处，均安全。 |
| [0020](0020-splitview-autosave-initial-positions.md) | NSSplitView：autosave 之上的「仅首次」分隔线初始位置 | Implemented | 「第一次按设计稿摆好分隔线、之后一律听用户的」是每个多栏窗口都要写一遍的需求，而 `autosaveName` 只给了后半段 —— 没有任何入口能插进「没存档时用我给的位置」，宿主只能自己去 `UserDefaults` 里摸 AppKit 的私有 key。本库已为 `NSWindow` 做过完全同构的 `restoreFrame(autosaveName:defaultSize:)`，`NSSplitView` 这侧还是空的。新增 `.box.restoreDividerPositions(autosaveName:initialPositions:)`，初始位置声明为 `.distanceFromStart` / `.distanceFromEnd` / `.fractionOfLength` / `.unchanged` 四种表达，其中 `.distanceFromEnd` 免掉手算 `bounds.width - x`，把宿主自找延迟时机的唯一理由一并收进 API。**没有 `@resultBuilder` 重载** —— 实测 result builder 闭包里的裸隐式成员链解析不出上下文类型（四种 builder 写法全失败），上了反而要在每行写全枚举名，故只留数组版。三条实测固化进实现：key 格式为 `"NSSplitView Subview Frames %@"`；恢复动作挂在 `setAutosaveName:` 的「值真的变了」分支里，故「先设 nil 再设名字」是必要的而非迷信；**「key 存在」不等于「恢复会成功」** —— `_walkLayoutDescriptionArray:` 在 `saved.count != arrangedSubviews.count` 时静默放弃恢复，于是 pane 数量变过之后，只看 key 的判据会既不恢复也不应用初始位置，落到默认均分。纯新增，不加 trait。下游 RuntimeViewer 是直接受益者，但本批次不改它。 |
| [0021](0021-spotlight-panel-replica.md) | SpotlightPanel：按二进制实测复刻 macOS 26 的 Spotlight 面板 | Implemented | 现有 `QuickActionBar` 的动画参数此前已逐位对齐 Spotlight，但骨架不是按 Spotlight 的架构长的 —— 定位用 `/1.3` 魔数、展开高度写死、尺寸变化无防抖、无位置记忆、键盘表只有四条，且缺一整层「内容要多高 → 窗口该多高」的契约（`SPSizingDelegate` + `ResultPlatterBehavior`）可以挂。本提案**不改 `QuickActionBar`**，在 `UIFoundationAppleInternal` 下按 Spotlight 26.6.2 二进制实测另起一个 `SpotlightPanel`：窗口与面板几何、完整动画（补上缺的那条 `_setContentBlurRadius` 曲线，spring(0.51, 0.05) → 25.0，比透明度慢近一倍所以观感是「先虚化后消失」）、内容驱动的尺寸策略、结果列表、13 条键盘表、搜索历史、过滤 token 栏、inline 补全、导航栈（复用 0001 而非仿 `NSPageController`）。**搜索内容由宿主提供**，不接后端、不做预览面板、不链 Apple 私有框架、不抢 ⌘Space、不引入 SwiftUI。新 trait `SpotlightPanel`（默认关闭，`enabledTraits` 连带 `AppleInternal` + `Navigation`），分三期交付，下游影响为零。 |
| [0022](0022-glass-effect-replica-view.md) | GlassEffectReplicaView：在 split view 的玻璃侧栏里复刻系统玻璃 | Implemented | macOS 26 起 split view 给 sidebar / inspector item 套的是 `NSGlassEffectView`（私有 `_variant` 17 / 18），颜色由窗口服务器实时合成、随窗口位置与激活状态漂移，任何 `NSColor` / material / 嵌套玻璃都只能接近（嵌套玻璃实测 rgb(41, 41, 45) 对 rgb(40, 40, 43)）。新增 `GlassEffectReplicaView`：自带一块玻璃、拷贝外层配置、把自己的 `CABackdropLayer` 塞进外层的 backdrop group —— 同组只采样一次，实测激活 / 非激活 / 移动 / 缩放四种状态下逐像素一致且不透明。配套暴露 `NSGlassEffectView` 的私有配置与 backdrop group 访问器。`_backdropGroupName` / `_groupIdentifier` 实测不通到 CA 层，已排除。落在既有 `AppleInternal` trait 下，不加 trait 不加依赖；下游 RuntimeViewer 用它做侧栏页面背景。配套使用指南见 [`GlassEffectReplica.md`](../GlassEffectReplica.md)，逆向依据见 [`Researchs/AppKit-NSGlassEffectView-SplitViewItem-Internals.md`](../../Researchs/AppKit-NSGlassEffectView-SplitViewItem-Internals.md)。 |
| [0023](0023-scroll-view-hides-visual-effect-view.md) | ScrollView：isHiddenVisualEffectView 更名为 hidesVisualEffectView | Implemented | `ScrollView` 上那个「隐藏 AppKit 插在 source-list 表格后面的 vibrancy 视图」的开关，名字既读不通（`is` + 形容词 + 名词，读作「是一个 hidden visual effect view」）又选错了词性 —— 它只在 `didAddSubview(_:)` 里被读到，只对**之后**加进来的 `NSVisualEffectView` 生效，是策略而非状态，而 Swift API Design Guidelines 把 `is…` 前缀留给断言当前状态的布尔量。更名为 `hidesVisualEffectView`，与 `NSScrollView` 自身的 `autohidesScrollers` / `drawsBackground` 同族。**行为一行不改**，也刻意不加 `didSet` 去作用于已存在的 subview —— 那是改语义，不是改名；旧名以 `@available(*, deprecated, renamed:)` 转发属性保留，下游只多一条警告，可按自己的节奏迁移。 |
| [0024](0024-component-layout-system.md) | UIFoundationComponent：并入 UIComponent 的声明式布局系统并补齐 AppKit 支持 | In Progress | 本库现有的两套布局都够不着「内容驱动的长列表」：`HStackView` / `VStackView` 走 Auto Layout，约束求解随子视图数量超线性增长且无视图复用、无可见区域裁剪；`GridView` 是固定网格。把 [lkzhao/UIComponent](https://github.com/lkzhao/UIComponent) `6.0.1` 那套**不用 Auto Layout、自己算 frame** 的声明式布局内核与渲染引擎（含 ID diff、reuse key、可见区域裁剪）搬进来，落成**独立 target + 独立 product** `UIFoundationComponent`，**不进伞包** —— 因此 `Spacer` 与 `Component` 两处重名都不必改。同时补齐它自诞生起就没有的 AppKit 支持：坐标系翻转、`NSView.layout()` 布局周期、`NSScrollView` 滚动容器、`TappableView` 的 AppKit 交互。不走现有那个 fork：实测 `AppKitSupportProgram/UIComponent` **两个平台都编译不过**（iOS 挂在 `typealias NSUIView = NSUIView` 的 circular reference，macOS 有 26 个裸 `import UIKit` 的文件从未适配），且跟一次上游要处理 52 个冲突、其中 22 个是纯 import 噪音。关键实测：渲染是**扁平**的（全部 renderable 挂在同一个容器上），所以坐标翻转只需一个 flipped 容器而非逐层转换；`AppKitPlus` 只提供 configuration / trait 体系，**不含** `layoutSubviews` / `isFlipped` / 手势的移植，帮不上忙。纯新增，三个已知下游零影响。不搬 `Text` / `Image` / `PrimaryMenu` / SwiftUI 集成，`HStackView` / `VStackView` 不废弃、两套并存。 |
| [draft](draft-component-example-port.md) | UIFoundationComponent：移植上游的教学式 Example，并补齐 TappableView | In Progress | 把上游 UIComponent `6.0.1`（与 `0024` 同一基线）那份 8638 行、18 章的教学式 Example 移植成 macOS 版塞进现有 demo browser，填上 `0024` 的落地步骤 7 —— 它的价值在形式：每个知识点「效果 + 它自己的源码」并排，而源码由 `#CodeExample` 宏在编译期把示例表达式**同时求值并捕获成文本**，所以展示的代码永远就是跑着的那段。**库侧只补一样：`TappableView`**（`0024` 落地步骤 5，规划做完了实现没写；它不是内容控件而是交互基础设施，`.tappableView { }` 让任意组件可点）。另两块都不进库：**叶子组件 `Text` / `Image` / `Separator` 由 Example 自建** —— 这个 target 交付的是布局系统，装什么由使用方定，`0024` 的非目标由此得到确认而非推翻；而自建的那个 `Text` 恰好是「怎么写叶子组件」的完整范例（TextKit 算尺寸、覆写 ascender/descender 做基线对齐、值类型 + `updateView` 以便复用、读环境键），值得单开一章讲。自建成立的技术前提已逐项核实：`EnvironmentKey` / `EnvironmentValues` / `CGSize.bound(to:)` / `boundWithAspectRatio(to:)` 均为 `public`，**一个缺口都没有**。**`updateProperties()` 用 AppKitPlus 现成的** —— 初稿曾计划自造，实测 `NSView (UpdateProperties)` 已提供三个方法且 macOS 14+ 带自动 Observation 追踪，形状与自造版完全一致而多做三件事，故取消自造；库不因此依赖 AppKitPlus，由 Example 自己 import（其工程本就开着该 trait）。追查这一条时**发现并更正了 `0024` 的一条错误调研结论**：「`extension AppKit.NSView` 一处都没有」实为 17 个 `NSView` category + 8 个 `NSViewController` category，根因是当初只验 `.swiftinterface` 而 **ObjC category 不出现在那里面**；原判断的另一半（无 `layoutSubviews` / `isFlipped` / `NSView` 的 `sizeThatFits` / 手势）逐项复核属实。顺带记下「直接放 `NSView` 实例」为什么不能替代 `Text`：尺寸确实算得对（`NSTextField.h:52`），但复用没了（持有实例时 id 固定为对象地址）、基线对齐静默算错（`ascender` 默认取 `size.height`）。纯新增，`Package.swift` 零改动（两个新依赖只进 Example 工程），三个已知下游零影响。不做 iOS Example。 |
